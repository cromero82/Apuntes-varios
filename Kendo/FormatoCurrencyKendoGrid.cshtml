columns.Bound(p => p.ARMAMENTO_INCAUTADO_ELEMENTO_CANTIDAD_INCAUTADA).Format("{0}").Width(200);
columns.Bound(p => p.ARMAMENTO_INCAUTADO_ELEMENTO_VALOR_ELEMENTO).Format("{0:C}").Width(200);

-- Para formato 2 decimales "{0:$ #,###.##}"
